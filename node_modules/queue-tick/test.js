const tape = require('tape')
const queueTick = require('queue-tick')
const js = require('queue-tick/queue-microtask')

tape('basic', function (t) {
  t.plan(2)

  queueTick(() => t.pass('tick'))
  js(() => t.pass('tock'))
})
